
// Update one record in your emails table in your robo-crm database filtering which item to update with email_id updating email_template_id, title, content fields with authentication and authorisation for root,admin roles
.arguments

   // email_id column value being primary key of record to update, mandatory argument
   email_id:long

   // email_template_id column's new value
   email_template_id:long

   // title column's new value
   title:string

   // content column's new value
   content:string

// Meta data description for endpoint
.description:Update one record in your emails table in your robo-crm database filtering which item to update with email_id updating email_template_id, title, content fields with authentication and authorisation for root,admin roles

// Type of endpoint
.type:crud-update

// Sanity checking invocation, making sure primary key(s) are specified.
validators.mandatory:x:@.arguments/*/email_id

// Verifying user is authorized to access endpoint.
auth.ticket.verify:root,admin
.foreign-keys
   .
      column:email_template_id
      table:email_templates
      foreign_column:email_template_id
      foreign_name:name
      long:bool:true

// Opening up our database connection.
data.connect:[generic|robo-crm]
   database-type:sqlite

   // Parametrising our invocation.
   add:x:./*/data.update/*/values
      get-nodes:x:@.arguments/*/[1,1000]
   add:x:./*/data.update/*/where/*/and
      get-nodes:x:@.arguments/*/[0,1]

   // Invoking our update slot.
   data.update
      database-type:sqlite
      table:emails
      values
      where
         and

   // Verifying record exists.
   if
      eq
         get-value:x:@data.update
         .:int:0
      .lambda
         response.status.set:404
         return
            result:Record not found

   // Returning the correct status code.
   response.status.set:202

   // Returning success to caller.
   unwrap:x:+/*
   return
      affected:x:@data.update
